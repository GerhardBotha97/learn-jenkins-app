pipeline {
    agent any

    stages {
        
        stage('Build Docker Image - Nightly') {
            steps {
                sh 'docker build -f ci/Dockerfile -t my-playwright .'
            }
        }
    }
}